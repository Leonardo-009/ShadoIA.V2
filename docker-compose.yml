services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_BACKEND_URL=http://backend:3001
    depends_on:
      - backend
    networks:
      - shadoia-network
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
      - FRONTEND_URL=http://localhost:3000
      - GOOGLE_GEMINI_API_KEY=AIzaSyAo-eYkft6CRP37FWoVf3cAoMW6Kc-Vzyk
      - VIRUSTOTAL_API_KEY=d35df5b3a1d7969d2dc93b6dbed5ade97ab81325346fccb3bc0b0df5a23a9a76
      - ABUSEIPDB_API_KEY=29572b812308a6bdaa13b55a8e27c23b9015f4de3553ef2aa6bdb81dbe64a194e91128bb7f5a55d4
      # Adicione suas variáveis de ambiente aqui
      # - OPENAI_API_KEY=your_openai_key
      # - AZURE_OPENAI_API_KEY=your_azure_key
      # - AZURE_OPENAI_ENDPOINT=your_azure_endpoint
      # - VIRUSTOTAL_API_KEY=your_virustotal_key
      # - ABUSEIPDB_API_KEY=your_abuseipdb_key
    networks:
      - shadoia-network
    restart: unless-stopped

networks:
  shadoia-network:
    driver: bridge 